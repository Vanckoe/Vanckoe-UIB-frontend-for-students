version: '3'

services:

  app:
    build:
      context: .
      dockerfile: _docker/Dockerfile
    volumes:
      - ./:/app
    ports:
      - "8081:8081"
    container_name: front_app
